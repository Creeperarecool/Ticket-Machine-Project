
/**
 * Write a description of class Date here.
 *
 * @author (your name)
 * @version (a version number or a date)
 */
public class Date
{
    // instance variables - replace the example below with your own
    private String month;
    private int day;
    private int year;
    
    public Date(String currentMonth, int currentDay, int currentYear)
    {
        month = currentMonth;
        day = currentDay;
        year = currentYear;
    }
}